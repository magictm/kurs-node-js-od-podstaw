// src/index.js

console.log('Witaj w głównym projekcie kursu Node.js!');
console.log("Środowisko przygotowane, plik uruchomiony z folderu 'src'.");

const currentTime = new Date().toLocaleTimeString();
console.log(`Aktualny czas serwera (przy uruchomieniu): ${currentTime}`);
